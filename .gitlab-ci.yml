stages:
    - run_tox

# before_script:
#     - apt-get update -qy
#     - apt-get install -y software-properties-common
#     - add-apt-repository -y ppa:deadsnakes/ppa
#     - apt-get update -qy
#     - apt-get install -y python3.8 python3-dev python3-pip tox

run_tox:
    image: python:3.8-slim
    stage: run_tox
    script:
        - apt-get install -y tox
        - tox